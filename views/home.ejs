<%- include("partials/header") -%>

    <h1>Home</h1>
    <p><%= StartingContent %></p>

    <% posts.forEach(function(post){ %>
     <h1><%= post.title %></h1>
     <% let postContent= post.content.slice(0,100) + " ..." %> 
     <p><%= postContent %>  <a href='/posts/<%= post.title %>'>Read more</a></p>  
    <% }) %>


<%- include("partials/footer") -%>
